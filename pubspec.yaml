name: animation_app
description: Artist focused 2D animation app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  perfect_freehand: ^2.0.1
  image: ^4.1.3
  ffmpeg_kit_flutter: ^6.0.3
  flex_color_picker: ^3.3.0
  just_audio: ^0.9.36
  share_plus: ^7.2.1
  permission_handler: ^11.1.0
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
